{
  "name": "miithii-api-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787 --var ALLOW_LOCAL_ORIGINS:true",
    "dev:preview": "wrangler dev --remote --ip 127.0.0.1 --port 8787",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "node test-protocol.mjs"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^3.0.45",
    "ai": "^7.0.94",
    "assistant-stream": "0.3.40",
    "openai": "^6.0.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "wrangler": "4.129.1"
  }
}
