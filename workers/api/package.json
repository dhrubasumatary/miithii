{
  "name": "miithii-api-worker",
  "private": true,
  "type": "module",
  "scripts": { "dev": "wrangler dev", "build": "wrangler deploy --dry-run --outdir dist", "deploy": "wrangler deploy", "types": "wrangler types" },
  "dependencies": { "assistant-stream": "0.3.40", "openai": "^6.0.0" },
  "devDependencies": { "wrangler": "4.129.1" }
}
